# docs

This folder is for supplementary documentation — architecture deep-dives, ADRs, runbooks — that
goes beyond what belongs in CLAUDE.md. Claude Code reads files here on demand when a task needs
them, not automatically on every session, so put material here that's useful context for specific
kinds of work rather than something every session needs up front.
